{
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.6.3",
    "gts": "^3.1.0",
    "@types/node": "^14.11.2"
  },
  "scripts": {
    "start": "node build/src/main.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
